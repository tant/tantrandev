# .env.sample
# Sao chép file này thành .env và điền các giá trị phù hợp cho từng biến môi trường.

# Token cho Hash Node (bắt buộc)
HASH_NODE_TOKEN=your_hash_node_token_here

# API key cho Gemini (bắt buộc)
GEMINI_API_KEY=your_gemini_api_key_here

# API key cho Brevo (nếu sử dụng các tính năng email, có thể để trống nếu không dùng)
BREVO_API_KEY=your_brevo_api_key_here
